var assert    = require('assert');
var importer  = require('../../lib/importers/node');

describe('sass-composer', function() {
  describe('importer', function() {
    describe('node', function () {

      it('', function(done) {
        importer();
      });

    });
  });
});
